import * as React from "react";
import { MinusIcon as RadixMinusIcon } from "@radix-ui/react-icons";
import { styled } from "../stitches.config";
import { SharedStyles } from "./Styles";

export const MinusIcon = styled(RadixMinusIcon, { ...SharedStyles });
